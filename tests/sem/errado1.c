set func(int a, int b){
    return a+b; // tipo de retorno incorreto.
}

int func(){
    float a;
    float b;

    // func retorna set, não definido.
    a = 5 + 2 * 3.0 / a + func(a,b,1,2) + z;
    
    return 0;
}